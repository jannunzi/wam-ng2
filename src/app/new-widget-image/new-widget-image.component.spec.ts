/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { NewWidgetImageComponent } from './new-widget-image.component';

describe('Component: NewWidgetImage', () => {
  it('should create an instance', () => {
    let component = new NewWidgetImageComponent();
    expect(component).toBeTruthy();
  });
});
